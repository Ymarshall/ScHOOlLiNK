{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cours_exo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
            <tr>
                <th>Nombrepage</th>
                <td>{{ entity.nombrePage }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ entity.fichier }}</td>
            </tr>
            <tr>
                <th>Dateenregistrement</th>
                <td>{{ entity.dateEnregistrement|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cours_exo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cours_exo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
