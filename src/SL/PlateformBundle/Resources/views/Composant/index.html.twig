{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Composant list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Heuredebut</th>
                <th>Heurefin</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('composant_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.heureDebut|date('H:i') }}</td>
                <td>{{ entity.heureFin|date('H:i') }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('composant_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('composant_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('composant_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
