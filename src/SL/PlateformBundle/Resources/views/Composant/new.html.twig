{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Composant creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('composant') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
