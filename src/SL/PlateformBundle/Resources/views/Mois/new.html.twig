{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Mois creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mois') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
