{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Emploi_temps creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('emploi_temps') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
