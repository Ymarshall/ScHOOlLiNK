{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Details_cr list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Objet</th>
                <th>Datemotif</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('details_cr_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.objet }}</td>
                <td>{% if entity.dateMotif %}{{ entity.dateMotif|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('details_cr_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('details_cr_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('details_cr_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
